---
- name: Restart chrony exporter
  ansible.builtin.systemd_service:
    name: chrony_exporter
    state: restarted
    daemon_reload: true

- name: Restart gpsd exporter
  ansible.builtin.systemd_service:
    name: gpsd_exporter
    state: restarted
    daemon_reload: true
