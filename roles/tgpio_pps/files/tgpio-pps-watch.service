# Managed by Ansible (roles/tgpio_pps).
[Unit]
Description=Re-arm Intel TGPIO PPS generator after driver self-disable
After=systemd-journald.service

[Service]
ExecStart=/usr/local/sbin/tgpio-pps-watch.sh
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
