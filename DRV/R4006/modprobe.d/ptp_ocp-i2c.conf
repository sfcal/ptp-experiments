# The Time Card driver creates the Xilinx I2C controller, EEPROM devices,
# I2C mux, and LED controller during probe, so these drivers must be ready.
softdep ptp_ocp pre: i2c-xiic at24 i2c-mux led-class-multicolor lm75 sht3x icp10100 timecard_leds
